# --- Dependencies ---
node_modules/
.pnp
.pnp.js

# --- Testing ---
coverage/

# --- Next.js ---
.next/
out/

# --- React Production Build ---
build/
dist/
tsconfig.tsbuildinfo

# --- Debug Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# --- Local Environment Files (React/Next) ---
.env.local
.env.development.local
.env.test.local
.env.production.local



# --- Go Build Output ---
# ชื่อไฟล์ binary ที่ได้จากการ go build (เปลี่ยนชื่อ 'my-app' เป็นชื่อแอปของคุณ)
# หรือถ้าคุณมักจะตั้งชื่อไฟล์ output เอง ให้ใส่ * ไปเลยเพื่อกันไฟล์ executable
*.exe
*.exe~
*.dll
*.so
*.dylib

# ถ้าคุณ output ไฟล์ binary ไปไว้ในโฟลเดอร์ bin/
/bin/

# --- Go Test ---
*.test
*.out

# --- Go Workspace (Local Development) ---
go.work
go.work.sum


# --- Secrets ---
.env
.env.production
.env.development
# ยกเว้น .env.example ที่ควรเอาขึ้นเพื่อให้คนอื่นรู้ว่าต้องใส่อะไรบ้าง
!*.example



# --- Nginx ---
nginx/
# logs ต่างๆ ไม่ควรเอาขึ้น
*.log
access.log
error.log

# --- SSL Certificates (ห้ามเอาขึ้นเด็ดขาด!) ---
*.pem
*.pub
*.key
*.crt
*.cer


# --- OS Files ---
.DS_Store
Thumbs.db

# --- IDE / Editors ---
.idea/
.vscode/
*.swp
*.swo

# --- Python ---
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# กฎ "lib/" ด้านบนมาจาก template ของ Python (ไว้กัน build artifact) แต่ไม่ได้ผูกกับ root
# จึงไป match frontend/src/lib/ ซึ่งเป็น source code จริงด้วย — ไฟล์ใหม่ในนั้นจะถูกซ่อนเงียบ ๆ
!frontend/src/lib/

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Jupyter Notebook
.ipynb_checkpoints

# --- เอกสารและของใช้ส่วนตัวระหว่างทำงาน (ไม่เอาขึ้นรีโป) ---
# เอกสารประกอบ/สไลด์/ไดอะแกรมที่ยังแก้กันอยู่ตลอด และไฟล์ .docx ที่ diff ไม่ได้
docs/
present_qa.md
present_script.md

# สคริปต์ช่วยงานที่รันในเครื่องตัวเอง ไม่ใช่ส่วนหนึ่งของ service
generate_go_proto.bat
test_chat.js
test_frontend_parse.js
marked_cdn.js
indices.json

# --- AI service: การวัดผล retrieval ---
# สคริปต์วัดผลกับผลลัพธ์ที่ได้ เป็นของชั่วคราวต่อรอบการทดลอง ค่าที่สรุปได้แล้ว
# ถูกเขียนไว้ในคอมเมนต์ของ app/config.py ซึ่งเป็นที่ที่คนอ่านโค้ดจะเจอจริง
backend/services/ai_service/scripts/
backend/services/ai_service/evalset/
backend/services/ai_service/EVAL.md
backend/services/ai_service/EVAL_RESULTS.xlsx
backend/services/ai_service/MANUAL.md